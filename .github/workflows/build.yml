on:
  push:
    branches:
      - jeremy

jobs:
  build:
    runs-on: windows-latest

    steps:
    - name: Checkout código fuente
      uses: actions/checkout@v2

    - uses: actions/setup-java@v3
      with:
        distribution: 'jdkfile'
        jdkFile: jdk-6u45-windows-x64.exe
        java-version: '1.6'
        architecture: x64
